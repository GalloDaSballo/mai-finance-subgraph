type Vault @entity {
  id: ID!
  account: Account!
  deposited: BigInt!
  borrowed: BigInt!
  closingFees: BigInt!
}

type Account @entity {
  id: ID!
  vaults: [Vault!]! @derivedFrom(field: "account")
}