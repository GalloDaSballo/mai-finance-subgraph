type Protocol @entity {
  id: ID!
  totalClosingFees: BigInt!
}

type Vault @entity {
  id: ID!
  account: Account!
  deposited: BigInt!
  borrowed: BigInt!
  closingFees: BigInt!
}

type Account @entity {
  id: ID!
  vaults: [Vault!]! @derivedFrom(field: "account")
}